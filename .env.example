# Server Configuration
PORT=3000
HOST=0.0.0.0
# SWAGGER_HOST=192.168.1.100 # (Opsional: Override host IP spesifik untuk Swagger UI jika diperlukan)

# Security / Auth Configuration
JWT_SECRET=your_jwt_secret_key_here
